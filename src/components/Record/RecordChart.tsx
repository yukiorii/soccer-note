import React from 'react';

import {
  VStack,
} from 'native-base';

const RecordChart = (props: any) => {
  const { inputValue, handleEventCardPress } = props

  return <VStack flex={1} mt={2}>
  </VStack>
}

export default RecordChart;